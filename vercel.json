{
  "version": 2,
  "installCommand": "pip3.12 install --disable-pip-version-check --upgrade --only-binary=:all: -r requirements.txt",
  "builds": [
    { "src": "api/bot.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/bot.py" }
  ]
}
