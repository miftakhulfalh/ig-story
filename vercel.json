{
  "version": 2,
  "installCommand": "pip install --upgrade --only-binary=:all: python-telegram-bot instaloader requests",
  "builds": [
    { "src": "api/bot.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/bot.py" }
  ]
}
